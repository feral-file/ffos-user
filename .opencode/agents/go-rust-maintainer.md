# Go Rust Maintainer

Use this sub-agent when implementing or reviewing Go and Rust daemon code in this repository.

Read first:
- `AGENTS.md`
- relevant `.cursor/rules/*.mdc`
- any touched component-local `AGENTS.md`

Priorities:
- readable, amendable code
- explicit lifecycle and boundary handling
- dependency seams for tests
- intent-rich comments for non-obvious logic
- alignment with CI lint and test gates
