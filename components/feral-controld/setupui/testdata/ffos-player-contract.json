{
  "contracts": {
    "contentPolicy": {
      "version": 1,
      "commands": [
        "getContentPolicy",
        "setContentPolicy"
      ],
      "notes": "These two commands return a Promise of the ordinary JSON-string reply envelope. Runtime.evaluate must use awaitPromise:true and returnByValue:true. getContentPolicy answers from the current snapshot WITHOUT waiting on a pending read: while the mirror is not yet applied it replies {ok:false,error:'contentPolicyUnavailable'} immediately, which is retryable, rather than holding the promise open. setContentPolicy resolves only once the write is durable. active:true means a policy is applied AND durable; active:false with playback continuing means the mirror could not be read and the built-in default (mature hidden, everything else plays) is in force until a later read or setContentPolicy succeeds \u2014 the daemon should repair it. Malformed policy returns invalidContentPolicy. blockUnratedCurated is an operator audit gate, not a controller setting. Missing cast contentContext means curated. contentRating is an OPEN vocabulary (dp1 3.3): only 'mature' hides anything, and any other string reads as unrated, so no document is refused and no item hidden over a label the player cannot read. This is a convenience viewing default, not tamper-resistant parental control."
    },
    "recentlyPlayed": {
      "version": 1,
      "commands": [
        "getRecentlyPlayed",
        "resolveRecentlyPlayed"
      ],
      "reply": {
        "status": [
          "ok",
          "empty",
          "error"
        ],
        "records": [
          "recordId",
          "playedAtMs",
          "isActive",
          "itemId",
          "title",
          "artist",
          "thumbnailUrl"
        ],
        "incomplete": "boolean",
        "activeOccurrenceKnown": "boolean"
      },
      "notes": "getRecentlyPlayed {} returns {ok,status,records,incomplete,activeOccurrenceKnown}. status is 'ok', 'empty' when nothing is retained, or 'error' with an explicit message \u2014 an unknown command or a transport failure must never be read as an empty history. Before the store has been read the reply is {ok:false,status:'error',error:'Recently played is still loading'}; the player reads it at boot so a controller should not normally see that. records[].isActive marks the retained record that is on screen NOW, and activeOccurrenceKnown false means the device cannot currently name one (a work committing, a stop, sleep, a failed render, or a policy change that moved the selection) \u2014 treat it as pending, not as 'nothing has ever played'. incomplete true means a committed work is missing from the timeline, including when the store itself was unreadable and was started over. resolveRecentlyPlayed {recordId} is INTERNAL to controld: it returns the retained item plus its defaults and contentContext so controld can re-enter its ordinary displayPlaylist path, which is what retires displayAt scheduler ownership. Do not treat it as a play command; there is no player-local replay. An evicted or malformed recordId is rejected before anything on the wall changes."
    },
    "displayPlaylistPolicy": {
      "version": 1,
      "requestFields": [
        "contentContext",
        "retireBlockedCurrent"
      ],
      "statusFields": [
        "contentContext"
      ],
      "errors": [
        "contentBlocked",
        "playlistInvalid",
        "contentPolicyUnavailable"
      ],
      "notes": "displayPlaylist.request.contentContext is 'curated' | 'personal', outside the signed DP-1 body. Absent means curated on a new cast; on a refresh it means UNCHANGED and inherits the cast's own origin, an explicit 'curated' narrows, and an explicit 'personal' is honoured only when the cast is already personal \u2014 a refresh can never invent a personal origin. checkStatus echoes the applied contentContext. retireBlockedCurrent:true is controld's refresh-only hint that it has already removed the blocked item from its own projection, so the current work must retire immediately instead of finishing its slot. contentBlocked means policy left nothing playable in the cast and the current work was NOT replaced; an empty item list is not contentBlocked, it clears playback. playlistInvalid means malformed input \u2014 a contentRating that is not a string, or a malformed contentReasons \u2014 and never an unrecognised rating value."
    },
    "mintPairingDisplay": {
      "version": 1,
      "requestKey": "request",
      "states": [
        "pairing_code",
        "request_received",
        "creating_token",
        "hidden"
      ],
      "acceptedResponse": {
        "ok": true
      }
    },
    "setupDisplay": {
      "version": 1,
      "requestKey": "request",
      "states": [
        "scanning",
        "softap_qr",
        "joining",
        "join_failed",
        "connecting",
        "setup_error",
        "updating",
        "finalizing",
        "claim_qr",
        "factory_reset",
        "ready",
        "hidden"
      ],
      "stateFields": {
        "softap_qr": {
          "required": [
            "ssid"
          ],
          "optional": [
            "password",
            "portal_url",
            "client_attached",
            "reason"
          ]
        },
        "join_failed": {
          "optional": [
            "reason"
          ]
        },
        "connecting": {
          "optional": [
            "reason"
          ]
        },
        "setup_error": {
          "optional": [
            "reason"
          ]
        },
        "updating": {
          "optional": [
            "progress"
          ]
        },
        "claim_qr": {
          "required": [
            "url"
          ],
          "optional": [
            "device_name"
          ]
        }
      },
      "notes": "Requests missing a required field for their state are rejected with {ok:false}. progress must be a finite number. The player renders overlay-driving commands (mintPairingDisplay, setupDisplay) without arbitration, so the controller must keep at most one non-hidden at a time.",
      "acceptedResponse": {
        "ok": true
      }
    },
    "renderStatus": {
      "version": 1,
      "notes": "checkStatus replies carry an optional numeric renderStatus: 0 pending, 1 loading, 2 ready, 3 failed. Live-only \u2014 never persisted, so boot and hydrate strip it. `ready` means the artwork finished loading AND is what the screen is showing, not that it rendered correctly. It publishes when the incoming slot commits, so on a transition it trails byte arrival by up to the crossfade duration (650ms); a first load publishes immediately. For iframe/PDF a browser-generated error document still reports ready, because Chromium does not emit an iframe error for a failed navigation. On a player build that declares this entry, an ABSENT renderStatus means 'no lifecycle published yet for the current mount' \u2014 not a terminal state, and not a fault. A manifest without this entry is an older build that never reports the field at all: controld must treat that as capability-absent rather than as a stuck pending, because the two are byte-identical on the wire (JSON.stringify drops the undefined key)."
    },
    "playerStatus": {
      "version": 1,
      "notes": "window.__ffosPlayerStatus() (no request payload \u2014 a plain global function, not a checkStatus-style command) returns JSON.stringify({protocol, route, handlerRegistered, hasArtwork, bootHydration}), where bootHydration is one of pending|ok|halted_cleared|halted_preserving|failed. Installed by CDPRequestHandler.initialize() and set back to null by cleanup(). Absent on player builds that predate this contract entry \u2014 controld's capability fuse treats a manifest without this entry as unsupported and falls back to conservative (log-only) boot recovery."
    }
  }
}
